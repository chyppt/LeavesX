From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: LeavesX Build <build@leavesx.local>
Date: Mon, 17 Aug 2026 07:08:28 +0800
Subject: [PATCH] Optimize villager farmland seed scans for bate102


diff --git a/net/minecraft/world/entity/ai/behavior/HarvestFarmland.java b/net/minecraft/world/entity/ai/behavior/HarvestFarmland.java
index 5a0d0d1c..f4937388 100644
--- a/net/minecraft/world/entity/ai/behavior/HarvestFarmland.java
+++ b/net/minecraft/world/entity/ai/behavior/HarvestFarmland.java
@@ -115,7 +115,9 @@ public class HarvestFarmland extends Behavior<Villager> {
                     } // CraftBukkit
                 }
 
-                if (blockState.isAir() && blockBelow instanceof FarmlandBlock && body.hasFarmSeeds()) {
+                // LeavesX start - the planting loop is already the exact seed-presence scan
+                if (blockState.isAir() && blockBelow instanceof FarmlandBlock
+                    && (org.leavesx.leavesx.config.LeavesXRuntime.villagerFarmlandSeedScanOptimization() || body.hasFarmSeeds())) {
                     SimpleContainer inventory = body.getInventory();
 
                     for (int i = 0; i < inventory.getContainerSize(); i++) {
@@ -149,6 +151,7 @@ public class HarvestFarmland extends Behavior<Villager> {
                         }
                     }
                 }
+                // LeavesX end - the planting loop is already the exact seed-presence scan
 
                 if (block instanceof CropBlock cropBlock && !cropBlock.isMaxAge(blockState)) {
                     this.validFarmlandAroundVillager.remove(this.aboveFarmlandPos);
