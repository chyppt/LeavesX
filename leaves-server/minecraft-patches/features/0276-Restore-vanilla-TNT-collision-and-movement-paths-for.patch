From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: LeavesX Build <build@leavesx.local>
Date: Sat, 22 Aug 2026 11:34:10 +0800
Subject: [PATCH] Restore vanilla TNT collision and movement paths for beta150


diff --git a/ca/spottedleaf/moonrise/patches/collisions/CollisionUtil.java b/ca/spottedleaf/moonrise/patches/collisions/CollisionUtil.java
index dd052a2abf21ac323dc73be41a6d7ae9ad4d6ddd..3dcb3f7a4828b9cb74fa5f0c1c6dcc7371bb8fdc 100644
--- a/ca/spottedleaf/moonrise/patches/collisions/CollisionUtil.java
+++ b/ca/spottedleaf/moonrise/patches/collisions/CollisionUtil.java
@@ -2096,45 +2096,31 @@ public final class CollisionUtil {
         // Vanilla for hard collisions has this backwards, and they expand by +epsilon but this causes terrible problems
         // specifically with boat collisions.
         aabb = aabb.inflate(-COLLISION_EPSILON, -COLLISION_EPSILON, -COLLISION_EPSILON);
-        final boolean reuseQueryBuffer = org.leavesx.leavesx.config.LeavesXRuntime.entityCollisionQueryBufferReuseOptimization();
-        final net.minecraft.world.level.Level.LeavesXEntityQueryBuffer queryBuffer = reuseQueryBuffer
-            ? net.minecraft.world.level.Level.leavesX$acquireEntityQueryBuffer()
-            : null;
-        try {
-            final List<Entity> entities;
-            if (entity != null && ((ChunkSystemEntity)entity).moonrise$isHardColliding()) {
-                entities = queryBuffer == null
-                    ? world.getEntities(entity, aabb, predicate)
-                    : queryBuffer.query(world, entity, aabb, predicate);
-            } else {
-                entities = queryBuffer == null
-                    ? ((ChunkSystemEntityGetter)world).moonrise$getHardCollidingEntities(entity, aabb, predicate)
-                    : queryBuffer.queryHardColliding(world, entity, aabb, predicate);
-            }
-
-            for (int i = 0, len = entities.size(); i < len; ++i) {
-                final Entity otherEntity = entities.get(i);
+        final List<Entity> entities;
+        if (entity != null && ((ChunkSystemEntity)entity).moonrise$isHardColliding()) {
+            entities = world.getEntities(entity, aabb, predicate);
+        } else {
+            entities = ((ChunkSystemEntityGetter)world).moonrise$getHardCollidingEntities(entity, aabb, predicate);
+        }
 
-                if (otherEntity.isSpectator()) {
-                    continue;
-                }
+        for (int i = 0, len = entities.size(); i < len; ++i) {
+            final Entity otherEntity = entities.get(i);
 
-                if ((entity == null && otherEntity.canBeCollidedWith(entity)) || (entity != null && entity.canCollideWith(otherEntity))) {
-                    if (checkOnly) {
-                        return true;
-                    } else {
-                        into.add(otherEntity.getBoundingBox());
-                        ret = true;
-                    }
-                }
+            if (otherEntity.isSpectator()) {
+                continue;
             }
 
-            return ret;
-        } finally {
-            if (queryBuffer != null) {
-                queryBuffer.close();
+            if ((entity == null && otherEntity.canBeCollidedWith(entity)) || (entity != null && entity.canCollideWith(otherEntity))) {
+                if (checkOnly) {
+                    return true;
+                } else {
+                    into.add(otherEntity.getBoundingBox());
+                    ret = true;
+                }
             }
         }
+
+        return ret;
     }
 
     public static boolean getCollisions(final Level world, final Entity entity, final AABB aabb,
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index 73a096fcc461683c10f390eb81b4255ca04c5b6a..3e5524f247b549148ff14ce80997904542ad85a5 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -280,7 +280,6 @@ public abstract class Entity
     public float yRotO;
     public float xRotO;
     private AABB bb = INITIAL_AABB;
-    private boolean boundingBoxChanged = false; // Leaves - skip entity move if movement is zero
     public boolean onGround;
     public boolean horizontalCollision;
     public boolean verticalCollision;
@@ -1150,13 +1149,6 @@ public abstract class Entity
     // Paper end - detailed watchdog information
 
     public void move(final MoverType moverType, Vec3 delta) {
-        // Leaves start - skip entity move if movement is zero
-        if (org.leavesmc.leaves.LeavesConfig.performance.skipEntityMoveIfMovementIsZero) {
-            if (!this.boundingBoxChanged && delta.equals(Vec3.ZERO)) {
-                return;
-            }
-        }
-        // Leaves end - skip entity move if movement is zero
         final Vec3 originalMovement = delta; // Paper - Expose pre-collision velocity
         // Paper start - detailed watchdog information
         ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread("Cannot move an entity off-main");
@@ -1296,17 +1288,8 @@ public abstract class Entity
                     }
                 }
 
-                // Leaves start - skip negligible planar movement multiplication
-                Vec3 oldDeltaMovement = this.getDeltaMovement();
-                if (!org.leavesmc.leaves.LeavesConfig.performance.skipNegligiblePlanarMovementMultiplication ||
-                        (oldDeltaMovement.x < -1e-6 || oldDeltaMovement.x > 1e-6 || oldDeltaMovement.z < -1e-6 || oldDeltaMovement.z > 1e-6)) {
-                    float blockSpeedFactor = this.getBlockSpeedFactor();
-                    if (!org.leavesmc.leaves.LeavesConfig.performance.skipNegligiblePlanarMovementMultiplication ||
-                            (blockSpeedFactor < 1 - 1e-6 || blockSpeedFactor > 1 + 1e-6)) {
-                        this.setDeltaMovement(this.getDeltaMovement().multiply(blockSpeedFactor, 1.0, blockSpeedFactor));
-                    }
-                }
-                // Leaves end - skip negligible planar movement multiplication
+                float blockSpeedFactor = this.getBlockSpeedFactor();
+                this.setDeltaMovement(this.getDeltaMovement().multiply(blockSpeedFactor, 1.0, blockSpeedFactor));
                 profiler.pop();
             }
         }
@@ -1830,9 +1813,8 @@ public abstract class Entity
                 } else {
                     VoxelShape intersectShape = state.getEntityInsideCollisionShape(this.level(), blockIntersection, this);
                     boolean insideBlock = intersectShape == Shapes.block()
-                        || this.collidedWithShapeMovingFrom(from, to, intersectShape.move(blockIntersection));
-                    FluidState fluidState = state.getFluidState();
-                    boolean insideFluid = !fluidState.isEmpty() && this.collidedWithFluid(fluidState, blockIntersection, from, to);
+                        || this.collidedWithShapeMovingFrom(from, to, intersectShape.move(new Vec3(blockIntersection)).toAabbs());
+                    boolean insideFluid = this.collidedWithFluid(state.getFluidState(), blockIntersection, from, to);
                     if ((insideBlock || insideFluid) && visitedBlocks.add(blockIntersection.asLong())) {
                         if (insideBlock) {
                             try {
@@ -1852,7 +1834,7 @@ public abstract class Entity
 
                         if (insideFluid) {
                             effectCollector.advanceStep(iteration, blockIntersection); // Paper - track position inside effect was triggered on
-                            fluidState.entityInside(this.level(), blockIntersection, this, effectCollector);
+                            state.getFluidState().entityInside(this.level(), blockIntersection, this, effectCollector);
                         }
 
                         if (debugEntityBlockIntersections) {
@@ -1884,7 +1866,7 @@ public abstract class Entity
 
     public boolean collidedWithFluid(final FluidState fluidState, final BlockPos blockPos, final Vec3 from, final Vec3 to) {
         AABB fluidAABB = fluidState.getAABB(this.level(), blockPos);
-        return fluidAABB != null && this.collidedWithShapeMovingFrom(from, to, fluidAABB);
+        return fluidAABB != null && this.collidedWithShapeMovingFrom(from, to, List.of(fluidAABB));
     }
 
     public boolean collidedWithShapeMovingFrom(final Vec3 from, final Vec3 to, final List<AABB> aabbs) {
@@ -1893,20 +1875,6 @@ public abstract class Entity
         return boundingBoxAtFrom.collidedAlongVector(travelVector, aabbs);
     }
 
-    public final boolean collidedWithShapeMovingFrom(final Vec3 from, final Vec3 to, final AABB aabb) {
-        AABB boundingBoxAtFrom = this.makeBoundingBox(from);
-        Vec3 travelVector = to.subtract(from);
-        return boundingBoxAtFrom.collidedAlongVector(travelVector, aabb);
-    }
-
-    public final boolean collidedWithShapeMovingFrom(final Vec3 from, final Vec3 to, final VoxelShape shape) {
-        // Simple collision shapes already expose a cached AABB; avoid materialising a one-element list for every entity move.
-        AABB singleAABB = shape.moonrise$getSingleAABBRepresentation();
-        return singleAABB != null
-            ? this.collidedWithShapeMovingFrom(from, to, singleAABB)
-            : this.collidedWithShapeMovingFrom(from, to, shape.toAabbs());
-    }
-
     protected void onInsideBlock(final BlockState state) {
     }
 
@@ -4599,7 +4567,6 @@ public abstract class Entity
     }
 
     public final void setBoundingBox(final AABB bb) {
-        if (!this.bb.equals(bb)) this.boundingBoxChanged = true; // Leaves - skip entity move if movement is zero
         // CraftBukkit start - block invalid bounding boxes
         double minX = bb.minX,
                 minY = bb.minY,
diff --git a/net/minecraft/world/level/EntityGetter.java b/net/minecraft/world/level/EntityGetter.java
index 23a477a9ab6ce9597b0497a6b800c090413403d3..85ee9caf3bc2ad31f715f941868e0c7ff2cfe146 100644
--- a/net/minecraft/world/level/EntityGetter.java
+++ b/net/minecraft/world/level/EntityGetter.java
@@ -87,42 +87,28 @@ public interface EntityGetter extends ca.spottedleaf.moonrise.patches.chunk_syst
         // specifically with boat collisions.
         testArea = testArea.inflate(-ca.spottedleaf.moonrise.patches.collisions.CollisionUtil.COLLISION_EPSILON, -ca.spottedleaf.moonrise.patches.collisions.CollisionUtil.COLLISION_EPSILON, -ca.spottedleaf.moonrise.patches.collisions.CollisionUtil.COLLISION_EPSILON);
 
-        final boolean reuseQueryBuffer = org.leavesx.leavesx.config.LeavesXRuntime.entityCollisionQueryBufferReuseOptimization();
-        final Level.LeavesXEntityQueryBuffer queryBuffer = reuseQueryBuffer && this instanceof Level
-            ? Level.leavesX$acquireEntityQueryBuffer()
-            : null;
-        try {
-            final List<Entity> entities;
-            if (source != null && ((ca.spottedleaf.moonrise.patches.chunk_system.entity.ChunkSystemEntity)source).moonrise$isHardColliding()) {
-                entities = queryBuffer == null
-                    ? this.getEntities(source, testArea, null)
-                    : queryBuffer.query((Level)this, source, testArea, null);
-            } else {
-                entities = queryBuffer == null
-                    ? ((ca.spottedleaf.moonrise.patches.chunk_system.world.ChunkSystemEntityGetter)this).moonrise$getHardCollidingEntities(source, testArea, null)
-                    : queryBuffer.queryHardColliding((Level)this, source, testArea, null);
-            }
-
-            final List<VoxelShape> ret = new java.util.ArrayList<>(Math.min(25, entities.size()));
+        final List<Entity> entities;
+        if (source != null && ((ca.spottedleaf.moonrise.patches.chunk_system.entity.ChunkSystemEntity)source).moonrise$isHardColliding()) {
+            entities = this.getEntities(source, testArea, null);
+        } else {
+            entities = ((ca.spottedleaf.moonrise.patches.chunk_system.world.ChunkSystemEntityGetter)this).moonrise$getHardCollidingEntities(source, testArea, null);
+        }
 
-            for (int i = 0, len = entities.size(); i < len; ++i) {
-                final Entity otherEntity = entities.get(i);
+        final List<VoxelShape> ret = new java.util.ArrayList<>(Math.min(25, entities.size()));
 
-                if (otherEntity.isSpectator()) {
-                    continue;
-                }
+        for (int i = 0, len = entities.size(); i < len; ++i) {
+            final Entity otherEntity = entities.get(i);
 
-                if ((source == null && otherEntity.canBeCollidedWith(source)) || (source != null && source.canCollideWith(otherEntity))) {
-                    ret.add(Shapes.create(otherEntity.getBoundingBox()));
-                }
+            if (otherEntity.isSpectator()) {
+                continue;
             }
 
-            return ret;
-        } finally {
-            if (queryBuffer != null) {
-                queryBuffer.close();
+            if ((source == null && otherEntity.canBeCollidedWith(source)) || (source != null && source.canCollideWith(otherEntity))) {
+                ret.add(Shapes.create(otherEntity.getBoundingBox()));
             }
         }
+
+        return ret;
         // Paper end - optimise collisions
     }
 
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index e9367b47aade862c6378063ac1f38d39d30c4943..46cb1fb7f086c26c98be16776a31d26eaadb687c 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -432,21 +432,6 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         return ret;
     }
 
-    /**
-     * Fills a caller-owned list with hard-colliding entities without changing the public query allocation contract.
-     * The caller must keep the list private until the collision calculation has completed.
-     */
-    public final void leavesX$getHardCollidingEntities(
-        final Entity entity,
-        final AABB box,
-        final Predicate<? super Entity> predicate,
-        final List<Entity> into
-    ) {
-        Profiler.get().incrementCounter("getEntities");
-        ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemLevel)this)
-            .moonrise$getEntityLookup().getHardCollidingEntities(entity, box, into, predicate);
-    }
-
     @Override
     public LevelChunk moonrise$getFullChunkIfLoaded(final int chunkX, final int chunkZ) {
         return (LevelChunk)this.getChunkSource().getChunk(chunkX, chunkZ, ChunkStatus.FULL, false);
@@ -2459,18 +2444,6 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
             return this.results;
         }
 
-        /** Collects hard-colliding entities directly into the reusable storage. */
-        public List<Entity> queryHardColliding(
-            final Level level,
-            final @Nullable Entity except,
-            final AABB boundingBox,
-            final Predicate<? super Entity> predicate
-        ) {
-            this.results.clear();
-            level.leavesX$getHardCollidingEntities(except, boundingBox, predicate, this.results);
-            return this.results;
-        }
-
         /** Collects one entity class into the reusable storage while preserving the typed Level query order. */
         @SuppressWarnings({"rawtypes", "unchecked"})
         public <T extends Entity> List<T> queryOfClass(
diff --git a/net/minecraft/world/phys/AABB.java b/net/minecraft/world/phys/AABB.java
index 8fcb4a5b38f90487c81299ebef662a2c6eec23e3..efb79617cebe7234d85721da7b7f1fd6ef1a6a73 100644
--- a/net/minecraft/world/phys/AABB.java
+++ b/net/minecraft/world/phys/AABB.java
@@ -405,18 +405,6 @@ public class AABB {
         return false;
     }
 
-    public final boolean collidedAlongVector(final Vec3 vector, final AABB aabb) {
-        // Fluids and simple voxel shapes have one box, so use the same collision math without allocating a temporary list.
-        Vec3 from = this.getCenter();
-        Vec3 to = from.add(vector);
-        AABB inflated = aabb.inflate(this.getXsize() * 0.5 - 1.0E-7, this.getYsize() * 0.5 - 1.0E-7, this.getZsize() * 0.5 - 1.0E-7);
-        if (inflated.contains(to) || inflated.contains(from)) {
-            return true;
-        }
-
-        return inflated.clip(from, to).isPresent();
-    }
-
     public double distanceToSqr(final Vec3 point) {
         double dx = Math.max(Math.max(this.minX - point.x, point.x - this.maxX), 0.0);
         double dy = Math.max(Math.max(this.minY - point.y, point.y - this.maxY), 0.0);
