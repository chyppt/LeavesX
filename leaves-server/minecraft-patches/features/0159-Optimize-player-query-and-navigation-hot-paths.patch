From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: LeavesX Build <build@leavesx.local>
Date: Sat, 15 Aug 2026 17:10:00 +0800
Subject: [PATCH] Optimize player query and navigation hot paths


diff --git a/net/minecraft/world/entity/ai/navigation/PathNavigation.java b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
index 1eb0afab932c3828a1f19861994b63b247df0fbb..ab0fe98ea774904b5e7b33d104c70d652c0835e5 100644
--- a/net/minecraft/world/entity/ai/navigation/PathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
@@ -299,13 +299,28 @@ public abstract class PathNavigation {
     protected void followThePath() {
         Vec3 mobPos = this.getTempMobPos();
         this.maxDistanceToWaypoint = this.mob.getBbWidth() > 0.75F ? this.mob.getBbWidth() / 2.0F : 0.75F - this.mob.getBbWidth() / 2.0F;
-        Vec3i currentNodePos = this.path.getNextNodePos();
-        double xDistance = Math.abs(this.mob.getX() - (currentNodePos.getX() + 0.5));
-        double yDistance = Math.abs(this.mob.getY() - currentNodePos.getY());
-        double zDistance = Math.abs(this.mob.getZ() - (currentNodePos.getZ() + 0.5));
-        boolean isCloseEnoughToCurrentNode = xDistance < this.maxDistanceToWaypoint && zDistance < this.maxDistanceToWaypoint && yDistance < 1.0;
-        if (isCloseEnoughToCurrentNode || this.canCutCorner(this.path.getNextNode().type) && this.shouldTargetNextNodeInDirection(mobPos)) {
-            this.path.advance();
+        if (org.leavesx.leavesx.config.LeavesXRuntime.pathfindingOptimizations()) {
+            // LeavesX start - Path#getNextNodePos converts the node to a new BlockPos on every navigation tick.
+            final Node currentNode = this.path.getNextNode();
+            final double xDistance = Math.abs(this.mob.getX() - (currentNode.x + 0.5));
+            final double yDistance = Math.abs(this.mob.getY() - currentNode.y);
+            final double zDistance = Math.abs(this.mob.getZ() - (currentNode.z + 0.5));
+            final boolean isCloseEnoughToCurrentNode = xDistance < this.maxDistanceToWaypoint
+                && zDistance < this.maxDistanceToWaypoint
+                && yDistance < 1.0;
+            if (isCloseEnoughToCurrentNode || this.canCutCorner(currentNode.type) && this.shouldTargetNextNodeInDirection(mobPos)) {
+                this.path.advance();
+            }
+            // LeavesX end - Path#getNextNodePos converts the node to a new BlockPos on every navigation tick.
+        } else {
+            Vec3i currentNodePos = this.path.getNextNodePos();
+            double xDistance = Math.abs(this.mob.getX() - (currentNodePos.getX() + 0.5));
+            double yDistance = Math.abs(this.mob.getY() - currentNodePos.getY());
+            double zDistance = Math.abs(this.mob.getZ() - (currentNodePos.getZ() + 0.5));
+            boolean isCloseEnoughToCurrentNode = xDistance < this.maxDistanceToWaypoint && zDistance < this.maxDistanceToWaypoint && yDistance < 1.0;
+            if (isCloseEnoughToCurrentNode || this.canCutCorner(this.path.getNextNode().type) && this.shouldTargetNextNodeInDirection(mobPos)) {
+                this.path.advance();
+            }
         }
 
         this.doStuckDetection(mobPos);
@@ -457,6 +472,14 @@ public abstract class PathNavigation {
             return false;
         } else if (this.path != null && !this.path.isDone() && this.path.getNodeCount() != 0) {
             Node target = this.path.getEndNode();
+            if (org.leavesx.leavesx.config.LeavesXRuntime.pathfindingOptimizations()) {
+                // LeavesX - retain the exact midpoint arithmetic while avoiding a temporary Vec3 allocation.
+                final double middleX = (target.x + this.mob.getX()) / 2.0;
+                final double middleY = (target.y + this.mob.getY()) / 2.0;
+                final double middleZ = (target.z + this.mob.getZ()) / 2.0;
+                final double remainingNodes = this.path.getNodeCount() - this.path.getNextNodeIndex();
+                return pos.distToCenterSqr(middleX, middleY, middleZ) < Mth.square(remainingNodes);
+            }
             Vec3 middlePos = new Vec3((target.x + this.mob.getX()) / 2.0, (target.y + this.mob.getY()) / 2.0, (target.z + this.mob.getZ()) / 2.0);
             return pos.closerToCenterThan(middlePos, this.path.getNodeCount() - this.path.getNextNodeIndex());
         } else {
diff --git a/net/minecraft/world/level/EntityGetter.java b/net/minecraft/world/level/EntityGetter.java
index cc938f43c4a5e947ed87ca2001cbaca4084bf64c..25216f2c2c35ad089c9bea04c05d21e2b7de5798 100644
--- a/net/minecraft/world/level/EntityGetter.java
+++ b/net/minecraft/world/level/EntityGetter.java
@@ -119,20 +119,55 @@ public interface EntityGetter extends ca.spottedleaf.moonrise.patches.chunk_syst
     // Paper end - Affects Spawning API
 
     default @Nullable Player getNearestPlayer(final double x, final double y, final double z, final double range, final @Nullable Predicate<Entity> predicate) {
+        if (!org.leavesx.leavesx.config.LeavesXRuntime.aiOptimizations()) {
+            double best = -1.0;
+            Player result = null;
+
+            for (Player player : this.players()) {
+                if (predicate == null || predicate.test(player)) {
+                    double dist = player.distanceToSqr(x, y, z);
+                    if ((range < 0.0 || dist < range * range) && (best == -1.0 || dist < best)) {
+                        best = dist;
+                        result = player;
+                    }
+                }
+            }
+
+            return result;
+        }
+
+        // LeavesX start - remove iterator allocation and repeated range multiplication from per-mob despawn checks
+        // The -1 sentinel and strict comparisons intentionally retain Paper's tie, NaN, and unlimited-range behavior.
+        final List<? extends Player> players = this.players();
+        final boolean unlimitedRange = range < 0.0;
+        final double rangeSquared = range * range;
         double best = -1.0;
         Player result = null;
 
-        for (Player player : this.players()) {
-            if (predicate == null || predicate.test(player)) {
-                double dist = player.distanceToSqr(x, y, z);
-                if ((range < 0.0 || dist < range * range) && (best == -1.0 || dist < best)) {
+        if (predicate == null) {
+            for (int index = 0, size = players.size(); index < size; index++) {
+                final Player player = players.get(index);
+                final double dist = player.distanceToSqr(x, y, z);
+                if ((unlimitedRange || dist < rangeSquared) && (best == -1.0 || dist < best)) {
                     best = dist;
                     result = player;
                 }
             }
+        } else {
+            for (int index = 0, size = players.size(); index < size; index++) {
+                final Player player = players.get(index);
+                if (predicate.test(player)) {
+                    final double dist = player.distanceToSqr(x, y, z);
+                    if ((unlimitedRange || dist < rangeSquared) && (best == -1.0 || dist < best)) {
+                        best = dist;
+                        result = player;
+                    }
+                }
+            }
         }
 
         return result;
+        // LeavesX end - remove iterator allocation and repeated range multiplication from per-mob despawn checks
     }
 
     // Paper start
@@ -169,6 +204,23 @@ public interface EntityGetter extends ca.spottedleaf.moonrise.patches.chunk_syst
     }
 
     default boolean hasNearbyAlivePlayerThatAffectsSpawning(double x, double y, double z, double range) {
+        if (org.leavesx.leavesx.config.LeavesXRuntime.aiOptimizations()) {
+            // LeavesX - keep predicate-before-distance ordering while using the world's stable indexed player list.
+            final List<? extends Player> players = this.players();
+            final boolean unlimitedRange = range < 0.0;
+            final double rangeSquared = range * range;
+            for (int index = 0, size = players.size(); index < size; index++) {
+                final Player player = players.get(index);
+                if (EntitySelector.PLAYER_AFFECTS_SPAWNING.test(player)) {
+                    final double playerDist = player.distanceToSqr(x, y, z);
+                    if (unlimitedRange || playerDist < rangeSquared) {
+                        return true;
+                    }
+                }
+            }
+            return false;
+        }
+
         for (Player player : this.players()) {
             if (EntitySelector.PLAYER_AFFECTS_SPAWNING.test(player)) { // combines NO_SPECTATORS and LIVING_ENTITY_STILL_ALIVE with an "affects spawning" check
                 double playerDist = player.distanceToSqr(x, y, z);
@@ -182,6 +234,22 @@ public interface EntityGetter extends ca.spottedleaf.moonrise.patches.chunk_syst
     // Paper end - Affects Spawning API
 
     default boolean hasNearbyAlivePlayer(final double x, final double y, final double z, final double range) {
+        if (org.leavesx.leavesx.config.LeavesXRuntime.aiOptimizations()) {
+            final List<? extends Player> players = this.players();
+            final boolean unlimitedRange = range < 0.0;
+            final double rangeSquared = range * range;
+            for (int index = 0, size = players.size(); index < size; index++) {
+                final Player player = players.get(index);
+                if (EntitySelector.NO_SPECTATORS.test(player) && EntitySelector.LIVING_ENTITY_STILL_ALIVE.test(player)) {
+                    final double playerDist = player.distanceToSqr(x, y, z);
+                    if (unlimitedRange || playerDist < rangeSquared) {
+                        return true;
+                    }
+                }
+            }
+            return false;
+        }
+
         for (Player player : this.players()) {
             if (EntitySelector.NO_SPECTATORS.test(player) && EntitySelector.LIVING_ENTITY_STILL_ALIVE.test(player)) {
                 double playerDist = player.distanceToSqr(x, y, z);
