From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: LeavesX Build <build@leavesx.local>
Date: Sun, 16 Aug 2026 21:22:55 +0800
Subject: [PATCH] Add armor stand placement query coverage for bate62


diff --git a/src/test/java/org/leavesx/leavesx/performance/ArmorStandPlacementEntityQueryHotPathTest.java b/src/test/java/org/leavesx/leavesx/performance/ArmorStandPlacementEntityQueryHotPathTest.java
new file mode 100644
index 0000000000000000000000000000000000000000..6875c36d06122a1052dd337bc265ba3c1aa9a0d7
--- /dev/null
+++ b/src/test/java/org/leavesx/leavesx/performance/ArmorStandPlacementEntityQueryHotPathTest.java
@@ -0,0 +1,51 @@
+package org.leavesx.leavesx.performance;
+
+import static org.junit.jupiter.api.Assertions.assertTrue;
+
+import java.io.IOException;
+import java.util.Arrays;
+import java.util.List;
+import net.minecraft.world.item.ArmorStandItem;
+import org.bukkit.support.environment.Normal;
+import org.junit.jupiter.api.Test;
+import org.objectweb.asm.ClassReader;
+import org.objectweb.asm.Opcodes;
+import org.objectweb.asm.tree.ClassNode;
+import org.objectweb.asm.tree.MethodInsnNode;
+import org.objectweb.asm.tree.MethodNode;
+
+/** Verifies armor stand placement can stop after the first occupying entity. */
+@Normal
+class ArmorStandPlacementEntityQueryHotPathTest {
+
+    @Test
+    void placementKeepsBoundedAndVanillaEntityQueries() throws IOException {
+        final MethodNode useOn = method(classNode(ArmorStandItem.class), "useOn");
+        final List<MethodInsnNode> calls = invocations(useOn);
+
+        assertTrue(calls.stream().anyMatch(call -> call.name.equals("entityQueryOptimizations")));
+        assertTrue(calls.stream().anyMatch(call -> call.name.equals("leavesX$hasEntity")));
+        assertTrue(calls.stream().anyMatch(call -> call.name.equals("getEntities")));
+        assertTrue(calls.stream().anyMatch(call -> call.name.equals("noCollision")));
+    }
+
+    private static List<MethodInsnNode> invocations(final MethodNode method) {
+        return Arrays.stream(method.instructions.toArray())
+            .filter(MethodInsnNode.class::isInstance)
+            .map(MethodInsnNode.class::cast)
+            .toList();
+    }
+
+    private static ClassNode classNode(final Class<?> owner) throws IOException {
+        final ClassNode classNode = new ClassNode(Opcodes.ASM9);
+        new ClassReader(owner.getName()).accept(classNode, ClassReader.SKIP_DEBUG | ClassReader.SKIP_FRAMES);
+        return classNode;
+    }
+
+    private static MethodNode method(final ClassNode owner, final String methodName) {
+        return owner.methods.stream()
+            .filter(method -> method.name.equals(methodName))
+            .findFirst()
+            .orElseThrow(() -> new AssertionError("Missing method " + owner.name + '#' + methodName));
+    }
+}
